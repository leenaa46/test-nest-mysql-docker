{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/nestjs-docker/package.json",
                "localRoot": "${workspaceFolder}/nestjs-docker"
            }
        }
    ]
}